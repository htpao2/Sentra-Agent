{
  "zh": "产出计划：请仅输出且仅输出一个工具调用块，使用 Sentra XML 协议格式：\n<sentra-tools>\n  <invoke name=\"emit_plan\">\n    <parameter name=\"plan\">{\"overview\": \"...\", \"steps\": [{\"aiName\": \"...\", \"reason\": [\"原因1\", \"原因2\"], \"nextStep\": \"...\", \"draftArgs\": {...}, \"dependsOn\": [...]}]}</parameter>\n  </invoke>\n</sentra-tools>\n\n格式说明（Sentra XML 协议）：\n- 使用 <invoke name=\"emit_plan\"> 标签包裹工具调用\n- plan 参数是复杂对象，使用 JSON 格式\n- **overview 字段（可选）**：简洁说明任务总体目标和执行策略\n- **reason 字段（必填，数组）**：每项为简短的调用理由，说明「为什么需要调用这个工具」\n- **nextStep 字段（必填）**：描述如何使用本步骤的执行结果\n- draftArgs 中的字符串值不要进行 JSON 转义，直接保留原始内容\n\n严格约束：\n- aiName 必须从允许列表中选择：{{allowed_list}}\n- steps 中每步仅包含一个工具，必须包含 draftArgs\n- reason 严格要求数组格式\n{{require_line}}\n- 禁止使用 Markdown 代码块围栏\n- 禁止输出多个 <sentra-tools> 块\n- 仅输出上述块，不要输出任何解释性文字\n\n计划结构示例：\n{{schema_hint}}",
  "en": "Produce ONE AND ONLY ONE plan tool call using Sentra XML Protocol format:\n<sentra-tools>\n  <invoke name=\"emit_plan\">\n    <parameter name=\"plan\">{\"overview\": \"...\", \"steps\": [{\"aiName\": \"...\", \"reason\": [\"reason1\", \"reason2\"], \"nextStep\": \"...\", \"draftArgs\": {...}, \"dependsOn\": [...]}]}</parameter>\n  </invoke>\n</sentra-tools>\n\nFormat specification (Sentra XML Protocol):\n- Use <invoke name=\"emit_plan\"> tag to wrap the tool call\n- plan parameter is a complex object, use JSON format\n- **reason field STRICTLY requires array format**\n- String values in draftArgs should NOT be JSON-escaped\n\nStrict constraints:\n- aiName must be chosen from: {{allowed_list}}\n- Each step uses exactly one tool and includes draftArgs\n- **reason STRICTLY requires array format**\n{{require_line}}\n- DO NOT use Markdown code fences\n- DO NOT output multiple <sentra-tools> blocks\n- Output only the block, no extra text\n\nPlan structure example:\n{{schema_hint}}"
}