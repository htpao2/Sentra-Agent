# ==================== Suno 音乐生成插件配置 ====================

# SUNO_API_BASE_URL：OpenAI 格式 API Base URL
SUNO_API_BASE_URL=https://api.openai.com/v1

# SUNO_API_KEY：OpenAI API Key
SUNO_API_KEY=your-api-key-here

# SUNO_MODEL：用于音乐生成的模型名称
SUNO_MODEL=suno-v5-free

# SUNO_TIMEOUT_MS：请求超时时间（毫秒，生成较久时可适当调大）
SUNO_TIMEOUT_MS=360000

# SUNO_DEFAULT_COVER_URL：默认音乐封面图片链接
SUNO_DEFAULT_COVER_URL=https://filesystem.site/cdn/20251003/lSl1Vi7WNkZzBm6lNJ3LxvxkaHJ77M.png

# WS_SDK_URL：WebSocket SDK 地址（用于发送音乐卡片）
WS_SDK_URL=ws://127.0.0.1:6702

# WS_SDK_TIMEOUT_MS：WebSocket 请求超时时间（毫秒）
WS_SDK_TIMEOUT_MS=15000

# WS_SDK_ARG_STYLE：WebSocket 参数风格（如 pair）
WS_SDK_ARG_STYLE=pair

# WS_SDK_SEND_PATH：通用发送路径前缀（留空则使用私聊/群聊专用路径）
WS_SDK_SEND_PATH=

# WS_SDK_SEND_PATH_PRIVATE：私聊发送路径
WS_SDK_SEND_PATH_PRIVATE=send.private

# WS_SDK_SEND_PATH_GROUP：群聊发送路径
WS_SDK_SEND_PATH_GROUP=send.group

# PLUGIN_ENABLED：是否启用本插件
PLUGIN_ENABLED=true

# SUNO_STREAM_ENABLED：是否启用流式输出
SUNO_STREAM_ENABLED=true
