# 是否启用插件
PLUGIN_ENABLED=true

# 插件超时时间，毫秒
PLUGIN_TIMEOUT_MS=150000

# 识图地址
VISION_BASE_URL=https://yuanplus.chat/v1

# 识图密钥
VISION_API_KEY=sk-REPLACE_ME

# 识图模型
VISION_MODEL=gemini-flash-latest

# 是否转换 GIF 格式为 JPEG（某些模型不支持原生 GIF）
VISION_CONVERT_GIF=false