# Per-plugin configuration for image_vision_read
VISION_BASE_URL=https://yuanplus.chat/v1
VISION_API_KEY=sk-REPLACE_ME
VISION_MODEL=gpt-4.1-mini

# 是否转换 GIF 格式为 JPEG（某些模型不支持原生 GIF）
# 设置为 true 则转换，false 则保持原生 GIF 格式
# 支持原生 GIF 的模型（如 gpt-4o, claude-3.5-sonnet）可设为 false
VISION_CONVERT_GIF=false

# Optional: override plugin timeout (ms)
# Priority: .env.PLUGIN_TIMEOUT_MS > config.json.timeoutMs
#PLUGIN_TIMEOUT_MS=120000
