# Sentra Config UI 配置文件

# 服务端口
SERVER_PORT=7245

# 前端开发端口
CLIENT_PORT=7244

# Sentra Agent 根目录（相对于当前目录）
SENTRA_ROOT=..

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info

# CORS 允许的域名（开发模式下使用 * 或具体域名）
CORS_ORIGIN=*
